# {{product_display_name}} — 法务三件套托管方案

> 最终提审时需要 Privacy Policy URL 可公网访问（curl 200）。**尤其中国区发布：Vercel 默认域名 `*.vercel.app` 在国内被墙，curl 从大陆返回 connect timeout**，会导致 Apple 5.1.1 / 3.1.2a 拒审（Apple 审核 server 能访问，但中国用户访问不到）。

## 方案选择（按国内可访问性排序）

| 方案 | 国内可访问 | 成本 | 上线时间 | 适用场景 |
|------|----------|------|---------|---------|
| **公司官网 `/legal/`** | ✅ | 0 | 2~5 天（IT/运维协作）| 长期运营 / 多产品复用 / 企业合规优先 |
| **阿里云 OSS + CDN** | ✅ | ¥5~20/月 | 1~2 小时 | 中国区主发 + 出海，性能最好 |
| **Vercel + 自有域名 CNAME** | ✅ | 0（域名另计）| 1~2 小时 | 已有 Vercel 但需国内可访问 |
| Vercel 默认域名 | ❌ 国内被墙 | 0 | 5 分钟 | ⚠️ **仅海外发布** — 国内区 curl 不通会拒审 |

---

## 推荐方案 A — 公司官网 `/legal/`（强烈推荐）

**前提：** 公司有 `{{company.website}}` 官网且 IT/运维可协作。

```
{{company.website}}/legal/
├── {{product_slug}}/
│   ├── privacy-policy.html
│   └── terms-of-use.html
```

**最终 URL**：

- Privacy Policy: `{{company.website}}/legal/{{product_slug}}/privacy-policy.html`
- Terms of Use: `{{company.website}}/legal/{{product_slug}}/terms-of-use.html`

**操作步骤**：

1. 把 `legal/*.html` 发 IT/运维，申请放到官网 `/legal/{{product_slug}}/` 目录
2. 等 IT/运维上线后，本地 curl 验证：
   ```bash
   curl -sS -o /dev/null -w "%{http_code}\n" {{company.website}}/legal/{{product_slug}}/privacy-policy.html
   ```
3. 国内外网络都测（公司 VPN 外 + 国内 4G）

---

## 推荐方案 B — 阿里云 OSS + CDN

**前提：** 有阿里云账号 + 备案域名。

```bash
# 1. OSS 创建 bucket（例 "company-legal"），ACL 公共读
aliyun oss cp legal/privacy-policy.html oss://company-legal/{{product_slug}}/
aliyun oss cp legal/terms-of-use.html oss://company-legal/{{product_slug}}/

# 2. 绑定 CDN 加速域名（例 legal.{{company.website}}）+ 配置 HTTPS
# 3. 国内外双测 curl 200
```

**最终 URL**：`https://legal.{{company.website}}/{{product_slug}}/privacy-policy.html`

---

## 推荐方案 C — Vercel + CNAME 自有域名

**前提：** 有自有域名（如 `legal.{{company.website}}`）。

```bash
# 1. Vercel 部署
cd ~/.ae/legal
vercel deploy legal/  # 临时获得 https://legal-xxx.vercel.app/{{product_slug}}/privacy-policy.html

# 2. Vercel Dashboard → Settings → Domains → 添加 legal.{{company.website}}
# 3. DNS 服务商配置 CNAME 指向 Vercel
# 4. 等 SSL 证书自动签发（~5 分钟）
# 5. curl 验证（国内 + 海外）
```

**最终 URL**：`https://legal.{{company.website}}/{{product_slug}}/privacy-policy.html`

**⚠️ 注意**：如果只用 Vercel 默认域名（`*.vercel.app`），**国内用户将无法访问 Privacy Policy 链接**，Apple 审核 server 能访问（过审）但上架后用户点击报错。**不可接受**。

---

## ❌ 不推荐 — Vercel 默认域名（仅海外）

**仅当产品明确只发海外区、不发中国区**时可用：

```
https://legal-xxx.vercel.app/{{product_slug}}/privacy-policy.html
```

国内 curl 会 timeout。**中国区发布必拒审**（Apple 审核 server 在海外能过，但中国区用户看不到 Privacy Policy 属 Apple 5.1.1 / 3.1.2a 违规）。

---

## 最终 URL（PM 填写）

- **Privacy Policy URL**: `<PM 填写>`
- **Terms of Use URL**: `<PM 填写>`
- **方案选择**: `A / B / C / D（仅海外）` — `<PM 勾选>`
- **国内 curl 200 验证**: `<Y / N>`（`curl --connect-timeout 10 {URL}` 不超时 + 返回 200）
- **海外 curl 200 验证**: `<Y / N>`

**所有项齐全 + 双测通过** 才能进入 `/ae-asc-submit`。

---

## 多产品复用

公司法务主体（`company.json`）跨产品复用，每个产品只需在 `{{product_slug}}/` 子目录下独立托管。新产品无需重新申请托管方案，只需加一个子目录。
