//
//  MainPageLoadWork.swift
//  {{PRODUCT_NAME}}
//
//  来源: bible-ios-template / 2026-04-15
//  Work Chain 最后一步 — 主界面加载 (TS-022)
//

import UIKit
import BCUtils

class MainPageLoadWork: WorkVoidCallbackTask {
    func work(_ callback: @escaping VoidCallback) {
        Self.showMain()
        callback()
    }
}

extension MainPageLoadWork {
    static func showMain() {
        guard let window = KeyWindow() else { return }
        window.rootViewController = TabBarController()
    }
}
